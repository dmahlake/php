#!/usr/bin/php
<?php
/*$ch = curl_init();

curl_setopt($ch, CURLOPT_URL, 'http:/www.w3.org/');
curl_setopt($ch, CURLOPT_HEADER, 0);

$ch2 = curl_copy_handle($ch);
curl_exec($ch2);

curl_close($ch2);
curl_close($ch);

*/
$ch = phpinfo()
?>